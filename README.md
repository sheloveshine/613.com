` мета-теги. В Next.js это делается через компонент **``** из `next/head`. Пример (в `app/layout.js` или `pages/_app.js`): ```jsx import Head from "next/head"; export default function Layout({ children }) { return ( <> {children} ); } ``` ### Рекомендации: - **Title** до 60 символов, должен включать название бренда. - **Description** до 160 символов, краткое описание бренда/сайта. - Добавьте **Open Graph (og:) теги** для красивого отображения в соцсетях. - Для favicon используйте `/public/favicon.ico`. --- ## 💡 Дополнительно - При каждом `git push` в `main` проект будет автоматически пересобираться. - Настройки сборки можно менять в файле `vercel.json`. - Для аналитики можно подключить **Google Analytics** или **Yandex.Metrica**. --- 👨‍💻 Сделано для бренда **613** 
